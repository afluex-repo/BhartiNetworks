@model BhartiNetwork.Models.Admin

@{
    ViewBag.Title = "GetProjectList";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


@using (Html.BeginForm("GetProjectList", "Admin", FormMethod.Post, new { @id = "signupForm", enctype = "multipart/form-data" }))
{   <div class="row">
        <div class="col-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Project List</h4>
                    <p style="color:red;">
                        @TempData["Record"]
                    </p>
                    <div class="row" style="padding-bottom:20px;">
                        <div class="col-sm-10">
                        </div>
                        <div class="col-sm-2">
                            <a href="/Admin/Project" class="btn btn-primary" style="border-radius:100px;">Add New</a>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12">

                            <div class="table-responsive">
                                <table id="file_export" class="table table-striped table-bordered display">
                                    <thead>
                                        <tr>
                                            <th>S No.</th>
                                            <th>Project Name</th>
                                            <th>Date</th>
                                            <th>Details</th>
                                            <th>File</th>
                                            <th>Action</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @if (Model != null)
                                        {
                                            if (Model.lstProject != null)
                                            {
                                                int i = 1;
                                                foreach (var item in Model.lstProject)
                                                {
                                                    <tr>
                                                        <td>@i</td>
                                                        <td>@item.Name</td>
                                                        <td>@item.Date</td>
                                                        <td style="white-space:pre-line;">@item.Details</td>
                                                        <td><img src="@item.Image" style="height:50px;width:50px;" /></td>
                                                        <td>
                                                            <a href="/Admin/Project?Id=@item.ProjectId" class="btn btn-info btn-sm">Edit</a>
                                                            <a href="/Admin/ProjectDelete?Id=@item.ProjectId" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want delete this record ?')">Delete</a>
                                                        </td>
                                                    </tr>
                                                    i++;
                                                }
                                            }
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>
}

